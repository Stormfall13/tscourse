// Массивы и коржети

// Arrays
const numbersArray: number[] = [1, 2, 3, 4, 5];
const namesArray: string[] = ['Alice', 'Alex', 'Nick'];

// Mixed types
// const personArray: (string | number)[] = ["Alex", 30];

// Tuples
const personArray: [string, number] = ["Alex", 30];
// personArray[0] = 'adsad'

const cordinates: [number, number] = [10,20];
const cordinates3D : [number, number, number] = [1,2,3];

console.log(namesArray, personArray, cordinates3D);
